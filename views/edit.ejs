<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-8">
        <div class="flex justify-end w-full">
            <a class="bg-red-500 rounded-md text-sm px-3 py-2 inline-block" href="/logout">Log-out</a>
        </div>
        <h3 class="text-3xl mb-2">Edit your post here.</h3>
        <form action="/update/<%= post._id %>" method="post">
            <textarea placeholder="What's on your mind ? " class="p-3 w-1/3 resize-none bg-transparent border-2 border-zinc-800 rounded-md block" name="content">
                <%= post.content %>
            </textarea>
            <input type="submit" class="w-40 text-black text-sm px-3 py-2 bg-yellow-400 block rounded-md mt-2" value="Edit your post">
        </form>
    </div>
</body>
</html>